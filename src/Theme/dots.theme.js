import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#7E33E0",
      light: "#AF7BF2",
      dark: "#6F1CDB",
    }
  },
  error:{
      main:'pink'
  }
});
export default theme;
